openapi: 3.0.0
info:
  title: Logtopus Query API
  version: 1.0.0
  description: |
    API to query logs from various storage backends.
    Project repository: https://github.com/predatorx7/logtopus
  contact:
    name: Logtopus Maintainer
    url: https://github.com/predatorx7
  license:
    name: BSD-3-Clause license (https://opensource.org/licenses/BSD-3-Clause)
    url: https://github.com/predatorx7/logtopus/blob/main/LICENSE
  x-logo:
    url: '/logtopus.png'
    altText: 'Logtopus'

servers:
  - url: http://localhost:8081
    description: Query Service (Local)

paths:
  /v1/logs:
    get:
      summary: Query logs
      operationId: queryLogs
      parameters:
        - name: subscriber_type
          in: query
          required: true
          schema:
            type: string
            enum: [clickhouse, file]
            default: clickhouse
          description: Storage backend to query.
        - name: start_time
          in: query
          schema:
            type: string
            format: date-time
          description: Filter logs after this time (inclusive).
        - name: end_time
          in: query
          schema:
            type: string
            format: date-time
          description: Filter logs before this time (inclusive).
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
            maximum: 10000
          description: Maximum number of logs to return.
        - name: level
          in: query
          schema:
            type: string
          description: Filter by log level (case-insensitive).
        - name: search
          in: query
          schema:
            type: string
          description: Text search in message field (case-insensitive).
        - name: session_id
          in: query
          schema:
            type: string
          description: Filter by Session ID (exact match, case-insensitive).
        - name: client_id
          in: query
          schema:
            type: string
          description: Filter by Client ID (exact match, case-insensitive).
        - name: source
          in: query
          schema:
            type: string
          description: Filter by Source (contains, case-insensitive).
        - name: error
          in: query
          schema:
            type: string
          description: Filter by Error (contains, case-insensitive).
        - name: before_context
          in: query
          schema:
            type: integer
            minimum: 0
            maximum: 1000
          description: Number of lines to include before the match. Requires session_id or client_id.
        - name: after_context
          in: query
          schema:
            type: integer
            minimum: 0
            maximum: 1000
          description: Number of lines to include after the match. Requires session_id or client_id.
        - name: context
          in: query
          schema:
            type: integer
            minimum: 0
            maximum: 1000
          description: Number of lines to include before and after the match. Requires session_id or client_id.
      responses:
        '200':
          description: List of logs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: './openapi.base.yaml#/components/schemas/LogEntry'
        '400':
          description: Invalid parameters
        '500':
          description: Internal Server Error

  /status:
    get:
      summary: Get service status
      operationId: getStatus
      description: Returns internal metrics and uptime.
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                $ref: './openapi.base.yaml#/components/schemas/QueryStatusResponse'
